# GitHub Copilot Instructions for helpdesk-bot

## Обзор проекта

Это Telegram Support Bot (Бот технической поддержки) написанный на Python 3.12 с использованием современного асинхронного стека.

## Технологический стек

- **Язык:** Python 3.12
- **Telegram API:** aiogram 3.x (современная асинхронная библиотека)
- **База данных:** SQLite через aiosqlite
- **ORM:** SQLAlchemy 2.0 (Async)
- **Конфигурация:** Pydantic Settings
- **Тестирование:** pytest, pytest-asyncio, pytest-cov
- **Менеджер зависимостей:** uv
- **Деплой:** Docker & Docker Compose
- **Планировщик задач:** APScheduler

## Архитектура проекта

```
├── core/           # Ядро: конфигурация (config.py), константы, логирование
├── database/       # Модели SQLAlchemy и инициализация БД
├── handlers/       # Обработчики команд и сообщений Telegram
│   ├── telegram.py # Пользовательские хэндлеры
│   └── admin.py    # Административные хэндлеры
├── services/       # Бизнес-логика бота
│   ├── ticket_service.py  # Работа с тикетами
│   ├── user_service.py    # Управление пользователями
│   ├── faq_service.py     # FAQ система с кэшированием
│   └── scheduler.py       # Периодические задачи
├── middlewares/    # Middleware для aiogram
├── tests/          # Юнит-тесты с pytest
└── main.py         # Точка входа приложения
```

## Стандарты кодирования

### Общие принципы

1. **Асинхронность всегда:** Все I/O операции должны быть асинхронными (`async/await`)
2. **Type hints обязательны:** Используй аннотации типов для всех функций
3. **SQLAlchemy 2.0 стиль:** Используй новый синтаксис с `Mapped[]` и `mapped_column()`
4. **Pydantic для валидации:** Все настройки через Pydantic Settings

### Именование переменных

- Избегай односимвольных имён (`t` → `new_ticket`, `match` → `ticket_id_match`)
- Используй описательные имена для повышения читаемости кода
- Константы в `UPPER_CASE`, переменные в `snake_case`

### Пример кода

```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

async def get_user_by_external_id(
    session: AsyncSession,
    source: SourceType,
    external_id: int
) -> User | None:
    """Получить пользователя по внешнему ID из источника."""
    stmt = select(User).where(
        User.source == source,
        User.external_id == external_id
    )
    result = await session.execute(stmt)
    return result.scalar_one_or_none()
```

## Безопасность (КРИТИЧНО)

### HTML Injection Prevention

**ВСЕГДА** используй `html.escape()` для пользовательского ввода в Telegram сообщениях с `parse_mode='HTML'`:

```python
import html
from aiogram import types

# ✅ ПРАВИЛЬНО
await bot.send_message(
    chat_id=settings.TG_STAFF_CHAT_ID,
    text=f"<b>Новый тикет #{ticket.id}</b>\n"
         f"От: {html.escape(user.full_name or 'Аноним')}\n"
         f"Вопрос: {html.escape(ticket.question_text)}",
    parse_mode="HTML"
)

# ❌ НЕПРАВИЛЬНО (уязвимость к HTML-инъекциям)
await bot.send_message(
    chat_id=settings.TG_STAFF_CHAT_ID,
    text=f"<b>Новый тикет #{ticket.id}</b>\n"
         f"От: {user.full_name}\n"  # ОПАСНО!
         f"Вопрос: {ticket.question_text}",  # ОПАСНО!
    parse_mode="HTML"
)
```

### Обработка ошибок

**НИКОГДА** не используй пустые `try...except: pass` блоки. Всегда логируй ошибки:

```python
# ✅ ПРАВИЛЬНО
try:
    await bot.send_message(user_id, "Ваш тикет закрыт")
except Exception as e:
    logger.warning(f"Failed to notify user {user_id}: {e}")
    # Рассмотри использование retry queue для критичных уведомлений

# ❌ НЕПРАВИЛЬНО
try:
    await bot.send_message(user_id, "Ваш тикет закрыт")
except:
    pass  # Тихий провал - пользователь не узнает о закрытии!
```

## Работа с базой данных

### Паттерн сессий

Используй `DbSessionMiddleware` для автоматической инъекции сессий в хэндлеры:

```python
from aiogram import Router
from sqlalchemy.ext.asyncio import AsyncSession

router = Router()

@router.message(Command("start"))
async def cmd_start(message: types.Message, session: AsyncSession):
    # session инъектится автоматически через middleware
    user = await get_or_create_user(session, message.from_user)
    await session.commit()
```

### Миграции моделей

При изменении моделей SQLAlchemy:
1. Обнови модель в `database/models.py`
2. Создай скрипт миграции в корне или используй `migrate_db.py` как шаблон
3. Протестируй миграцию локально перед деплоем

### Оптимизация запросов

- Используй `.options(selectinload(...))` для eager loading связанных объектов
- Кэшируй часто запрашиваемые данные (пример: `FAQService.load_cache()`)
- Избегай N+1 запросов

## FAQ система (Производительность)

**Learning:** Для небольших коллекций (до ~100 элементов) простой цикл с `in` быстрее regex:

```python
# ✅ ОПТИМИЗИРОВАНО (на 46% быстрее)
message_lower = message.text.lower()  # Вычисли один раз!
for trigger_lower, answer in faq_cache:
    if trigger_lower in message_lower:
        return answer

# ❌ МЕДЛЕННЕЕ
for trigger, answer in faq_cache:
    if trigger.lower() in message.text.lower():  # .lower() каждый раз!
        return answer
```

## UX паттерны

### Блокирующие состояния

При блокировке действия ВСЕГДА предоставляй пользователю инструкцию "что делать дальше":

```python
# ✅ ПРАВИЛЬНО
await message.answer(
    f"⚠️ У вас уже есть активный тикет #{ticket.id}\n"
    f"Просто напишите сюда ваш вопрос, и я добавлю его к тикету."
)

# ❌ НЕПРАВИЛЬНО (тупик для пользователя)
await message.answer("❌ У вас уже есть активный тикет")
```

## Тестирование

### Запуск тестов

```bash
# Все тесты
uv run pytest

# С покрытием (минимум 88%)
uv run pytest --cov=. --cov-report=term-missing

# Конкретный файл
uv run pytest tests/test_ticket_service.py -v
```

### Структура тестов

```python
import pytest
from sqlalchemy.ext.asyncio import AsyncSession

@pytest.mark.asyncio
async def test_create_ticket(session: AsyncSession, test_user: User):
    """Тест создания тикета с корректными данными."""
    ticket = await create_ticket(
        session=session,
        user=test_user,
        question_text="Тестовый вопрос",
        category_id=1
    )
    
    assert ticket.id is not None
    assert ticket.status == TicketStatus.NEW
    assert ticket.user_id == test_user.id
```

### Покрытие кода

Исключения из покрытия (см. `pyproject.toml`):
- `tests/*` - тестовые файлы
- `main.py` - точка входа
- `*_script.py` - утилитарные скрипты

## Git workflow

### Commit messages

Используй осмысленные commit messages на русском или английском:

```
✅ Хорошо:
- "Добавлен HTML escape для предотвращения инъекций"
- "Оптимизирован поиск в FAQ кэше"
- "Исправлена обработка ошибок в ticket_service"

❌ Плохо:
- "fix"
- "update"
- "changes"
```

## CI/CD

Проект использует GitHub Actions (`.github/workflows/ci.yml`):
- ✅ Автоматические тесты при каждом push/PR
- ✅ Проверка покрытия кода (минимум 88%)
- ✅ Кэширование зависимостей uv

При добавлении новых фич убедись, что:
1. Все тесты проходят
2. Покрытие кода не упало ниже 88%
3. Нет конфликтов с основной веткой

## Docker

### Локальная разработка

```bash
# Запуск
docker-compose up -d --build

# Логи
docker-compose logs -f

# Остановка
docker-compose down
```

### Структура окружения

- База данных монтируется в `./data` (персистентность)
- Переменные окружения из `.env` файла
- Используется multi-stage build для минимизации образа

## Известные ограничения

1. **VK интеграция удалена:** Код содержит "ghost code" от VK (например, `SourceType.VK`). Не используй VK-специфические поля.
2. **Без Alembic:** Миграции БД делаются вручную через скрипты, не через Alembic.
3. **SQLite лимиты:** SQLite хорош для малых/средних нагрузок, но не поддерживает concurrent writes. Для масштабирования рассмотри PostgreSQL.

## Полезные команды

```bash
# Установка зависимостей
uv sync --all-extras --dev

# Запуск бота локально
python main.py

# Инициализация БД
python init_db_script.py

# Миграция БД
python migrate_db.py

# Форматирование (если настроено)
black .
ruff check .
```

## Дополнительные ресурсы

- [aiogram 3.x документация](https://docs.aiogram.dev/en/latest/)
- [SQLAlchemy 2.0 документация](https://docs.sqlalchemy.org/en/20/)
- [Pydantic Settings](https://docs.pydantic.dev/latest/concepts/pydantic_settings/)

---

**Помни:** Этот бот обрабатывает реальные запросы пользователей. Приоритет всегда на:
1. 🔒 **Безопасность** (XSS, инъекции, утечки данных)
2. 🎯 **UX** (понятные сообщения, помощь в тупиковых ситуациях)
3. ⚡ **Производительность** (асинхронность, кэширование, оптимизация запросов)
4. 🧪 **Тестируемость** (покрытие >88%, изолированные юнит-тесты)
