TG_BOT_TOKEN=test_token_12345
# Root Admin ID (User ID)
TG_ADMIN_ID=123456789
# Staff Group Chat ID (where tickets are sent). For supergroups, starts with -100
TG_STAFF_CHAT_ID=-100123456789
# OpenRouter API Key for LLM-based ticket summarization (optional)
OPENROUTER_API_KEY=sk-or-v1-...
# LLM Model Name (optional, defaults to google/gemini-3-flash-preview)
LLM_MODEL_NAME=google/gemini-3-flash-preview

# Database configuration
# Option 1: SQLite (default) - just specify the database file path
DB_NAME=/app/data/support.db

# Option 2: PostgreSQL - set DATABASE_URL (takes precedence over DB_NAME)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/helpdesk

# Telegram Mini App settings (optional)
# WEBAPP_HOST=0.0.0.0
# WEBAPP_PORT=8080
# WEBAPP_URL=https://your-domain.com/webapp
