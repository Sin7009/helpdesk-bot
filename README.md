# Telegram Support Bot

![CI Status](https://github.com/Sin7009/helpdesk-bot/workflows/CI/badge.svg)

Бот для организации технической поддержки. Проект написан на **Python 3.12** и использует современный асинхронный стек технологий.

## 🛠 Технологии

*   **Язык:** Python 3.12
*   **Telegram API:** [aiogram 3.x](https://github.com/aiogram/aiogram)
*   **База данных:** SQLite (через `aiosqlite`)
*   **ORM:** [SQLAlchemy 2.0](https://www.sqlalchemy.org/) (Async)
*   **Конфигурация:** [Pydantic Settings](https://docs.pydantic.dev/latest/concepts/pydantic_settings/)
*   **Деплой:** Docker & Docker Compose

## 🚀 Установка и запуск

### Предварительные требования

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/Sin7009/helpdesk-bot.git
    cd helpdesk-bot
    ```
2.  Создайте файл конфигурации:
    ```bash
    cp .env.example .env
    ```
3.  Заполните `.env` своими данными (получить токен можно у [@BotFather](https://t.me/BotFather)):
    ```ini
    TG_BOT_TOKEN=ваш_токен_телеграм
    TG_ADMIN_ID=123456789
    TG_STAFF_CHAT_ID=-100123456789
    # Опционально: путь к базе данных
    # DB_NAME=/app/data/support.db
    ```

### Способ 1: Запуск через Docker (Рекомендуется)

Самый простой способ запустить бота изолированно. База данных будет сохраняться в папке `./data` на хост-машине.

```bash
docker-compose up -d --build
```
*   `up` — запустить контейнеры
*   `-d` — в фоновом режиме (detached)
*   `--build` — пересобрать образ перед запуском

Посмотреть логи:
```bash
docker-compose logs -f
```

### Способ 2: Локальный запуск (Python)

Если вы хотите запустить бота без Docker для разработки.

1.  Создайте и активируйте виртуальное окружение:
    ```bash
    # Linux/macOS
    python3.12 -m venv .venv
    source .venv/bin/activate

    # Windows
    python -m venv .venv
    .venv\Scripts\activate
    ```

2.  Установите зависимости (проект использует `pyproject.toml`):
    ```bash
    pip install .
    # или, если используете uv
    uv sync
    ```

3.  Запустите бота:
    ```bash
    python main.py
    ```

## 🎓 Особенности для университетов

Бот включает специальные функции для работы в университетской среде:

### Система приоритетов
*   **Автоматическое определение** приоритета заявок (🔴 Срочно, 🟠 Высокий, 🟢 Обычный, ⚪ Низкий)
*   **Ключевые слова:** "срочно", "экзамен", "завтра" → Срочный приоритет
*   **Визуальные индикаторы** с эмодзи в уведомлениях

### Профиль студента
*   **Команды:** `/myprofile` (просмотр), `/updateprofile` (обновление)
*   **Информация:** студенческий билет, курс, факультет
*   **Контекст для администраторов** в каждой заявке

### SLA метрики и оценка качества
*   **Отслеживание времени ответа** — автоматический подсчёт среднего времени
*   **Оценка обслуживания** — система оценок 1-5 звезд после закрытия заявки
*   **Расширенная статистика** — приоритеты, SLA, средний рейтинг в ежедневном отчёте

📖 **Подробнее:** см. [UNIVERSITY_IMPROVEMENTS.md](UNIVERSITY_IMPROVEMENTS.md) и [QUICK_START.md](QUICK_START.md)

## 📂 Структура проекта

*   `core/` — ядро приложения: конфигурация (`config.py`) и логирование.
*   `database/` — работа с базой данных: модели SQLAlchemy и инициализация.
*   `handlers/` — обработчики сообщений и команд.
*   `services/` — бизнес-логика бота (включая `priority_service.py`).
*   `main.py` — точка входа.
*   `docker-compose.yml` — конфигурация контейнеризации.
*   `migrate_university_improvements.py` — миграция БД для новых функций.

## 🧪 Разработка и тестирование

Проект использует [uv](https://github.com/astral-sh/uv) для управления зависимостями и [pytest](https://pytest.org/) для тестирования.

### Запуск тестов

1.  Установите зависимости для разработки:
    ```bash
    uv sync --all-extras --dev
    ```

2.  Запустите тесты:
    ```bash
    uv run pytest
    ```

3.  Запустите тесты с покрытием кода:
    ```bash
    uv run pytest --cov=. --cov-report=term-missing
    ```

### CI/CD

Проект использует GitHub Actions для автоматического тестирования:
- Запуск всех тестов при каждом push и pull request
- Проверка покрытия кода (минимум 88%)
- Генерация отчетов о покрытии кода
- Кэширование зависимостей для ускорения сборки

## 🎯 Команды бота

### Для студентов
*   `/start` — начать работу с ботом и выбрать тему обращения
*   `/myprofile` — посмотреть свой профиль
*   `/updateprofile` — обновить информацию (студ. билет, курс, факультет)

### Для администраторов
*   `/reply ID текст` — ответить на заявку
*   `/close ID` — закрыть заявку
*   `/add_category название` — добавить новую категорию

### Интерактивные элементы
*   **Выбор категории** — кнопки для быстрого выбора темы
*   **FAQ** — частые вопросы с автоматическими ответами
*   **Оценка качества** — кнопки 1-5 звезд после закрытия заявки

## 📊 Примеры работы

### Создание заявки студентом
```
Студент: СРОЧНО! Не могу войти в личный кабинет, завтра экзамен!

Бот автоматически определяет приоритет: 🔴 СРОЧНО

Администратор получает:
━━━━━━━━━━━━━━━━━━━━━━
🔴 Новый запрос №5 (ID: #123)
От: Иван Петров
🎓 ID: 2024-12345, 3 курс, ФИТ
Тема: IT / ЛК | Приоритет: Срочно
Текст: СРОЧНО! Не могу войти...
━━━━━━━━━━━━━━━━━━━━━━
```

### Ежедневная статистика
```
📊 Статистика за 21.12.2025:

Общее:
Всего запросов: 15
Закрыто: 12

По приоритетам:
🔴 Срочно: 2
🟠 Высокий: 5
🟢 Обычный: 7
⚪ Низкий: 1

Топ тем:
1. IT / ЛК: 6
2. Учеба: 5
3. Справки: 4

SLA метрики:
Среднее время ответа: 45 мин
Средняя оценка: 4.3/5 ⭐⭐⭐⭐ (10 оценок)
```

## 🔄 Миграция и обновление

При первом запуске с новыми функциями выполните миграцию базы данных:

```bash
python migrate_university_improvements.py
```

Эта команда добавит новые поля:
- Приоритеты заявок
- Информацию о студентах
- Метрики SLA
- Оценки удовлетворённости

## 📚 Дополнительная документация

*   [QUICK_START.md](QUICK_START.md) — быстрый старт и примеры
*   [UNIVERSITY_IMPROVEMENTS.md](UNIVERSITY_IMPROVEMENTS.md) — полное руководство по университетским функциям
*   [IMPROVEMENTS_SUMMARY.md](IMPROVEMENTS_SUMMARY.md) — краткое резюме улучшений
*   [CONTRIBUTING.md](CONTRIBUTING.md) — руководство для разработчиков

